apiVersion: v1
kind: PersistentVolume
metadata:
  name: movies-mongo-pv
  labels:
    app: movies-mongo
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  hostPath:
    path: /mnt/movies-mongo-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: movies-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
  selector:
    matchLabels:
      app: movies-mongo