apiVersion: v1
kind: PersistentVolume
metadata:
  name: subscription-expiration-mongo-pv
  labels:
    app: subscription-expiration-mongo
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  hostPath:
    path: /mnt/subscription-expiration-mongo-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: subscription-expiration-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
  selector:
    matchLabels:
      app: subscription-expiration-mongo