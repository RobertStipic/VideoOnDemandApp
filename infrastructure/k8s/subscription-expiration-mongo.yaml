apiVersion: apps/v1
kind: Deployment
metadata:
  name: subscription-expiration-mongo-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: subscription-expiration-mongo
  template:
    metadata:
      labels:
        app: subscription-expiration-mongo
    spec:
      terminationGracePeriodSeconds: 60      
      initContainers:
        - name: remove-lockfile
          image: busybox
          command: ["sh", "-c", "rm -f /data/db/mongod.lock /data/db/WiredTiger.lock"]
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
        - name: fix-permissions
          image: busybox
          command: ["sh", "-c", "chown -R 999:999 /data/db && chmod -R u+rwX /data/db"]
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      containers:
        - image: mongo
          name: subscription-expiration-mongo
          lifecycle:
            preStop:
              exec:
                command: ["mongod", "--dbpath", "/data/db", "--shutdown"]
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      volumes:
        - name: mongo-storage
          persistentVolumeClaim:
            claimName: subscription-expiration-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: subscription-expiration-mongo-srv
spec:
  selector:
    app: subscription-expiration-mongo
  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017