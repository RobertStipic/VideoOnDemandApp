apiVersion: apps/v1
kind: Deployment
metadata:
  name: watch-history-mongo-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: watch-history-mongo
  template:
    metadata:
      labels:
        app: watch-history-mongo
    spec:
      initContainers:
        - name: remove-lockfile
          image: busybox
          command: ["sh", "-c", "rm -f /data/db/mongod.lock /data/db/WiredTiger.lock"]
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
        - name: fix-permissions
          image: busybox
          command: ["sh", "-c", "chown -R 999:999 /data/db && chmod -R u+rwX /data/db"]
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      containers:
        - image: mongo
          name: watch-history-mongo
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      volumes:
        - name: mongo-storage
          persistentVolumeClaim:
            claimName: watch-history-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: watch-history-mongo-srv
spec:
  selector:
    app: watch-history-mongo
  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017